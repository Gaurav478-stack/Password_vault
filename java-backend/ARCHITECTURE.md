# 🏗️ Architecture & Class Diagrams

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SecurePass Vault System                    │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────────┐
│   Frontend    │   │  Node.js API  │   │   Java Backend    │
│  (JavaScript) │   │   (Express)   │   │  (OOP Service)    │
└───────────────┘   └───────────────┘   └───────────────────┘
        │                     │                     │
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────────┐
│  localStorage │   │    MongoDB    │   │   In-Memory       │
│   (Browser)   │   │   (Database)  │   │   Storage         │
└───────────────┘   └───────────────┘   └───────────────────┘
```

## 🎯 Java Backend Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    SecurePassApplication                       │
│                      (Entry Point)                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ creates
                         ▼
          ┌──────────────────────────┐
          │      VaultManager         │◄──────── Facade Pattern
          │   (Main Orchestrator)     │
          └───────────┬───────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
         │ uses                    │ uses
         ▼                         ▼
┌──────────────────┐    ┌──────────────────┐
│IPasswordAnalyzer │    │  IAuditService   │◄─── Abstraction
│   (Interface)    │    │   (Interface)    │
└────────┬─────────┘    └────────┬─────────┘
         │                       │
         │ implements            │ implements
         ▼                       ▼
┌───────────────────┐   ┌──────────────────────┐
│AdvancedPassword   │   │InMemoryAuditService  │◄── Inheritance
│    Analyzer       │   │                      │
└───────────────────┘   └──────────────────────┘
```

## 📦 Class Hierarchy

### Model Layer (Data Objects)

```
┌────────────────┐
│    Password    │
├────────────────┤
│ - id: String   │
│ - service      │◄───── Encapsulation
│ - username     │       (Private fields)
│ - encrypted    │
│ - category     │
├────────────────┤
│ + getters()    │
│ + setters()    │◄───── Public methods
│ + recordAccess│
└────────────────┘

┌────────────────┐
│   AuditLog     │
├────────────────┤
│ - id: String   │
│ - action       │◄───── Immutable
│ - userId       │       (final fields)
│ - timestamp    │
│ - success      │
├────────────────┤
│ + Builder      │◄───── Builder Pattern
│ + getters()    │
└────────────────┘

┌─────────────────────┐
│PasswordStrengthResult│
├─────────────────────┤
│ - score: int        │
│ - level: String     │◄─── Data Transfer
│ - entropy: double   │     Object (DTO)
│ - feedback          │
├─────────────────────┤
│ + getters()         │
└─────────────────────┘
```

### Service Layer (Business Logic)

```
        ┌──────────────────┐
        │IPasswordAnalyzer │◄──── Interface (Abstraction)
        ├──────────────────┤
        │ + analyze()      │
        │ + generate()     │
        │ + validate()     │
        └────────┬─────────┘
                 │
                 │ implements
                 ▼
   ┌──────────────────────────┐
   │AdvancedPasswordAnalyzer  │◄─── Implementation
   ├──────────────────────────┤
   │ - COMMON_PASSWORDS       │
   │ - random: SecureRandom   │
   ├──────────────────────────┤
   │ + analyzeStrength()      │◄─── Override
   │ + generatePassword()     │
   │ + validatePassword()     │
   │ - calculateEntropy()     │◄─── Private helpers
   │ - hasSequentialChars()   │
   └──────────────────────────┘


        ┌──────────────────┐
        │  IAuditService   │◄──── Interface
        ├──────────────────┤
        │ + logEvent()     │
        │ + getLogs()      │
        │ + getReport()    │
        └────────┬─────────┘
                 │
                 │ implements
                 ▼
   ┌────────────────────────┐
   │InMemoryAuditService    │◄─── Implementation
   ├────────────────────────┤
   │ - auditLogs: List      │
   │ - formatter            │
   ├────────────────────────┤
   │ + logEvent()           │◄─── Override
   │ + getLogsByUser()      │
   │ + generateReport()     │
   └────────────────────────┘
```

### Manager Layer (Orchestration)

```
┌──────────────────────────────────────────┐
│           VaultManager                    │◄─── Composition
├──────────────────────────────────────────┤
│ - passwords: Map<String, Password>       │
│ - passwordAnalyzer: IPasswordAnalyzer    │◄─── Has-A
│ - auditService: IAuditService            │◄─── Has-A
│ - userId: String                         │
├──────────────────────────────────────────┤
│ + addPassword()                          │
│ + getPassword()                          │
│ + updatePassword()                       │
│ + deletePassword()                       │
│ + searchPasswords()                      │
│ + analyzePasswordStrength()              │◄─── Delegates
│ + generatePassword()                     │◄─── to services
│ + findWeakPasswords()                    │
│ + findDuplicatePasswords()               │
│ + getStatistics()                        │
│ - logAudit()                             │◄─── Private
└──────────────────────────────────────────┘
```

## 🔄 Interaction Flow

### Password Analysis Flow

```
User
  │
  │ 1. Enter password
  ▼
SecurePassApplication
  │
  │ 2. Call analyzePasswordStrength()
  ▼
VaultManager
  │
  │ 3. Delegate to analyzer
  ▼
AdvancedPasswordAnalyzer
  │
  ├─ 4a. Calculate score
  ├─ 4b. Calculate entropy
  ├─ 4c. Check patterns
  ├─ 4d. Generate feedback
  │
  │ 5. Return PasswordStrengthResult
  ▼
VaultManager
  │
  │ 6. Log audit event
  ▼
InMemoryAuditService
  │
  │ 7. Store audit log
  ▼
Return result to Application
```

### Vault Operation Flow

```
User Request
     │
     │ 1. addPassword(password)
     ▼
VaultManager
     │
     ├─ 2a. Validate password
     │      │
     │      ▼
     │  PasswordAnalyzer
     │      │
     │      └─ Return validation result
     │
     ├─ 2b. Store password
     │      │
     │      ▼
     │  passwords.put(id, password)
     │
     └─ 2c. Log audit event
            │
            ▼
     InMemoryAuditService
            │
            └─ Store AuditLog
```

## 🎨 Design Patterns Visualization

### Builder Pattern

```
AuditLog.Builder
      │
      ├─ .id("123")
      ├─ .action("LOGIN")
      ├─ .userId("user123")
      ├─ .success(true)
      │
      └─ .build()
            │
            ▼
      AuditLog (immutable)
```

### Dependency Injection

```
            Constructor
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
 userId    Analyzer      AuditService
    │            │            │
    └────────────┴────────────┘
                 │
                 ▼
          VaultManager
       (fully configured)
```

### Facade Pattern

```
Multiple Complex Subsystems:
┌────────────────────┐
│PasswordAnalyzer    │
│ - entropy calc     │
│ - pattern detect   │
│ - scoring          │
└────────────────────┘
┌────────────────────┐
│ AuditService       │
│ - logging          │
│ - filtering        │
│ - reports          │
└────────────────────┘
┌────────────────────┐
│ Storage            │
│ - CRUD ops         │
│ - searching        │
└────────────────────┘
         │
         │ Simplified through
         ▼
┌────────────────────┐
│   VaultManager     │◄─── Single entry point
│                    │
│ Simple methods:    │
│ - addPassword()    │
│ - searchPasswords()│
│ - analyze()        │
└────────────────────┘
```

## 📊 Data Flow Diagram

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ Request
       ▼
┌─────────────────┐
│ VaultManager    │
└────┬───────┬────┘
     │       │
     │       └─────────────┐
     │                     │
     ▼                     ▼
┌──────────────┐    ┌─────────────┐
│Password      │    │Audit        │
│Analyzer      │    │Service      │
└──────┬───────┘    └──────┬──────┘
       │                   │
       │ Analysis          │ Log
       │ Result            │ Event
       ▼                   ▼
┌──────────────┐    ┌─────────────┐
│Password      │    │Audit        │
│Strength      │    │Logs         │
│Result        │    │Collection   │
└──────────────┘    └─────────────┘
```

## 🔐 Security Architecture

```
┌─────────────────────────────────┐
│    SecureRandom Generator       │
└────────────┬────────────────────┘
             │
             │ Provides entropy
             ▼
┌─────────────────────────────────┐
│  Password Generator             │
└────────────┬────────────────────┘
             │
             │ Creates strong password
             ▼
┌─────────────────────────────────┐
│  Password Analyzer              │
└────────────┬────────────────────┘
             │
             │ Validates strength
             ▼
┌─────────────────────────────────┐
│  Vault Manager                  │
└────────────┬────────────────────┘
             │
             │ Stores securely
             ▼
┌─────────────────────────────────┐
│  Encrypted Storage              │
└─────────────────────────────────┘
             │
             │ Logs activity
             ▼
┌─────────────────────────────────┐
│  Audit Service                  │
└─────────────────────────────────┘
```

## 📈 Class Relationships Summary

| Relationship Type | Example | Pattern |
|------------------|---------|---------|
| **Has-A** | VaultManager has PasswordAnalyzer | Composition |
| **Is-A** | AdvancedPasswordAnalyzer is IPasswordAnalyzer | Inheritance |
| **Uses** | VaultManager uses Password model | Association |
| **Creates** | Builder creates AuditLog | Builder Pattern |
| **Depends on** | All depend on model classes | Dependency |

## 🎯 SOLID Principles Applied

```
S - Single Responsibility
    Each class has one reason to change
    ├─ Password: Manages password data
    ├─ PasswordAnalyzer: Analyzes passwords
    └─ AuditService: Handles logging

O - Open/Closed
    Open for extension, closed for modification
    └─ New analyzers can implement IPasswordAnalyzer

L - Liskov Substitution
    Derived classes are substitutable
    └─ Any IPasswordAnalyzer can replace another

I - Interface Segregation
    Specific interfaces for different needs
    ├─ IPasswordAnalyzer: Password operations
    └─ IAuditService: Audit operations

D - Dependency Inversion
    Depend on abstractions, not concretions
    └─ VaultManager depends on interfaces
```

---

**These diagrams show the clean architecture and professional OOP design of the Java backend!** 🎨
